#define N 4
#define MAX 2000
        int a[N][N]={{2, 12983, -2112104, -33215},
                     {1, 2121212, 3, 4},
                     {0, -11, 2, 121433},
                     {1, -9861, 17901, -1}};
        int b[N][N]={{19562, 243053, 4, 9423235},
                     {1, 1212, 1243, 23124},
                     {0, -21211, 2, -3233},
                     {-86756601, 2121, 3231, -2311}};
        int c[N][N];

_start()
{
	int i, j, k;
        int sum;

        for ( i = 0; i < N; ++i )
          for ( j = 0; j < N; ++j )
          {
            sum = 0;
            for ( k = 0; k < N; ++k)
              sum = sum + a[i][k] * b[k][j];
            c[i][j] = sum;
          }

}

